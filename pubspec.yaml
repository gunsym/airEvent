name: air_event
description: airEvent

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^6.0.1
  equatable: ^1.2.3
  flutter_bloc: ^6.1.0
  font_awesome_flutter: ^8.4.0
  formz: ^0.2.0
  google_fonts: ^1.1.0
  meta: ^1.1.8
  pedantic: ^1.9.0
  flutter_form_bloc: ^0.19.0
  authentication_repository:
    path: packages/authentication_repository
  members_repository:
    path: packages/members_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^7.0.0
  mockito: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
